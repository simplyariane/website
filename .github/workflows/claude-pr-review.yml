name: Claude PR Review

on:
  pull_request:
    types: [opened, synchronize]

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  review:
    uses: simplyariane/overseer/.github/workflows/claude-pr-review.reusable.yml@main
    secrets: inherit
